## PlatformIO environment

Global PlatformIO packages/toolchains are user-managed and may be shared by multiple firmware projects. Never repair, delete, prune, or reinstall the global `.platformio` environment automatically. For suspicious build state, clean or remove only the repo-local `.pio/` first. Global environment repair requires explicit user approval.